// Code generated by goa v3.16.1, DO NOT EDIT.
//
// clients HTTP client CLI support package
//
// Command:
// $ goa gen learngo.io/firstgoa/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
	clientc "learngo.io/firstgoa/gen/http/client/client"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() string {
	return `client (add|get|show)
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` client add --body '{
      "ClientName": "Pariatur consequatur reiciendis rerum similique aut dolore."
   }' --client-id "Est dolorem libero."` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		clientFlags = flag.NewFlagSet("client", flag.ContinueOnError)

		clientAddFlags        = flag.NewFlagSet("add", flag.ExitOnError)
		clientAddBodyFlag     = clientAddFlags.String("body", "REQUIRED", "")
		clientAddClientIDFlag = clientAddFlags.String("client-id", "REQUIRED", "Client ID")

		clientGetFlags        = flag.NewFlagSet("get", flag.ExitOnError)
		clientGetClientIDFlag = clientGetFlags.String("client-id", "REQUIRED", "Client ID")

		clientShowFlags = flag.NewFlagSet("show", flag.ExitOnError)
	)
	clientFlags.Usage = clientUsage
	clientAddFlags.Usage = clientAddUsage
	clientGetFlags.Usage = clientGetUsage
	clientShowFlags.Usage = clientShowUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "client":
			svcf = clientFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "client":
			switch epn {
			case "add":
				epf = clientAddFlags

			case "get":
				epf = clientGetFlags

			case "show":
				epf = clientShowFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "client":
			c := clientc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "add":
				endpoint = c.Add()
				data, err = clientc.BuildAddPayload(*clientAddBodyFlag, *clientAddClientIDFlag)
			case "get":
				endpoint = c.Get()
				data, err = clientc.BuildGetPayload(*clientGetClientIDFlag)
			case "show":
				endpoint = c.Show()
				data = nil
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// clientUsage displays the usage of the client command and its subcommands.
func clientUsage() {
	fmt.Fprintf(os.Stderr, `The Client service allows access to client members
Usage:
    %[1]s [globalflags] client COMMAND [flags]

COMMAND:
    add: Add implements add.
    get: Get implements get.
    show: Show implements show.

Additional help:
    %[1]s client COMMAND --help
`, os.Args[0])
}
func clientAddUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] client add -body JSON -client-id STRING

Add implements add.
    -body JSON: 
    -client-id STRING: Client ID

Example:
    %[1]s client add --body '{
      "ClientName": "Pariatur consequatur reiciendis rerum similique aut dolore."
   }' --client-id "Est dolorem libero."
`, os.Args[0])
}

func clientGetUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] client get -client-id STRING

Get implements get.
    -client-id STRING: Client ID

Example:
    %[1]s client get --client-id "Tempore recusandae qui dolor veritatis."
`, os.Args[0])
}

func clientShowUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] client show

Show implements show.

Example:
    %[1]s client show
`, os.Args[0])
}
